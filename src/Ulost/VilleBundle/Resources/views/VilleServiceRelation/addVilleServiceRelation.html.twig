{% extends "UlostObjectBundle::layout.html.twig" %}
 {% block Core_body %}
     <div>
         {% if service is defined and service is not empty %}
         <a href="{{ path('ulost_view_service', {'id':service.id}) }}" class="button">Retour au service {{ service.name }}</a>
         {% else %}
         <a href="{{ path('ulost_index_service') }}" class="button">Retour à la liste des services</a>
         {% endif %}
     </div>
     {{ include("UlostVilleBundle:VilleServiceRelation:formVilleServiceRelation.html.twig") }}

 {% endblock %}


