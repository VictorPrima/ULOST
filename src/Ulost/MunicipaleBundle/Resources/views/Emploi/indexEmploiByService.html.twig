{% extends "UlostCoreBundle::layout.html.twig" %}

{% block Core_body %}
    <div>
        <h2>
            Liste des emplois de l'utilisateur {{ service.name }}
        </h2>
    </div>
    <div>
        {{ render(controller("UlostMunicipaleBundle:Emploi:showEmploiByService", {'id':service.id})) }}
    </div>
{% endblock %}